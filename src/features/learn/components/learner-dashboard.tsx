import Link from "next/link";

export type LearnerDashboardCourse = { courseSlug: string; title: string; progressPercent: number; nextLessonSlug?: string; nextLessonTitle?: string };

export function LearnerDashboard({ learnerEmail, courses }: { learnerEmail: string; courses: readonly LearnerDashboardCourse[] }) {
  return <section className="mx-auto w-full max-w-5xl px-5 py-12 sm:px-8 sm:py-16"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-ktf-blue">Bespoke Learn</p><div className="mt-3 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-semibold tracking-[-0.035em] text-ktf-navy sm:text-4xl">Your learning</h1><p className="mt-2 text-sm text-ktf-gray-600">Signed in as {learnerEmail}</p></div><form action="/api/learn/auth/logout" method="post"><button className="inline-flex min-h-11 items-center rounded-md border border-ktf-gray-300 px-4 text-sm font-semibold text-ktf-gray-700 hover:border-ktf-blue/30 hover:text-ktf-blue focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ktf-blue">Sign out</button></form></div>{courses.length === 0 ? <div className="mt-10 max-w-2xl rounded-xl border border-dashed border-ktf-gray-300 bg-white p-7 sm:p-9"><h2 className="text-lg font-semibold text-ktf-navy">No courses are available to you yet.</h2><p className="mt-2 text-sm leading-6 text-ktf-gray-600">Reviewed courses will appear here when access is available to your learner account.</p><Link href="/courses" className="mt-5 inline-flex min-h-11 items-center rounded-md bg-ktf-blue px-4 text-sm font-semibold text-white hover:bg-ktf-blue-deep focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-ktf-blue">Browse courses</Link></div> : <ul className="mt-10 space-y-4">{courses.map((course) => <li key={course.courseSlug} className="rounded-xl border border-ktf-gray-200 bg-white p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-semibold text-ktf-navy">{course.title}</h2><p className="mt-2 text-sm text-ktf-gray-600">{course.progressPercent}% complete{course.nextLessonTitle ? ` · Next: ${course.nextLessonTitle}` : ""}</p></div><Link href={course.nextLessonSlug ? `/courses/${course.courseSlug}/lessons/${course.nextLessonSlug}` : `/courses/${course.courseSlug}/learn`} className="inline-flex min-h-11 items-center justify-center rounded-md bg-ktf-blue px-4 text-sm font-semibold text-white hover:bg-ktf-blue-deep">Continue</Link></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-ktf-gray-100"><div className="h-full bg-ktf-blue" style={{ width: `${Math.max(0, Math.min(100, course.progressPercent))}%` }} aria-label={`${course.progressPercent}% complete`} /></div></li>)}</ul>}</section>;
}
